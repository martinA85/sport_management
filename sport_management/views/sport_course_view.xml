<odoo>
    <data>
        <!-- sport.course form view -->
        <record id="sport_course_view_form" model="ir.ui.view">
            <field name="name">sport.course.view.form</field>
            <field name="model">sport.course</field>
            <field name="arch" type="xml">
                <form string="Course">
                    <sheet>
                        <group>
                            <field name="name"/>
                            <label for="len_hours" string="Durée : " />
                            <div class="hour_picker">
                                <field name="len_hours" style="width: 25%" placeholder="HH" />
                                <field name="len_mins" style="width: 25%" placeholder="MM" />
                            </div>
                            <field name="max_attendee" />
                        </group>
                    </sheet>
                </form>
            </field>
        </record>
    </data>
</odoo>