<?xml version="1.0" encoding="utf-8" ?>
<odoo>
    <data noupdate="1">
        <record id="sport_subscription_email_subscription" model="mail.template">
            <field name="name">sport.subscription.email_subscription</field>
            <field name="email_from">${object.company_id and object.company_id.email or ''}</field>
            <field name="subject">Your subscription has been registered</field>
            <field name="email_to">${object.email|safe}</field>
            <field name="lang">${object.lang}</field>
            <field name="model_id" ref="base.model_res_partner" />
            <field name="auto_delete" eval="False" />
            <field name="body_html">
                <![CDATA[
                    <p>
                        Dear ${(object.client_id.name)},
                        <br /><br />
                        You has been registered to the ${(object.name)} session at ${(object.session_id.start_date)} for ${(object.session_id.course_id.length)}.
                    </p>
                ]]>
            </field>
        </record>
    </data>
</odoo>